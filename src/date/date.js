import buttonExl from '../images/file.svg';
import buttonMenu from '../images/menu.svg';
import buttonSetings from '../images/setting.svg';
import buttonProfile from '../images/profile.svg';
import buttonNotifications from '../images/notifications.svg';

export const images = {
  buttonExl: buttonExl,
  buttonMenu: buttonMenu,
  buttonSetings: buttonSetings,
  buttonProfile: buttonProfile,
  buttonNotifications: buttonNotifications
}